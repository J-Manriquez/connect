<?xml version="1.0" encoding="utf-8"?>
<!-- Style 3: Circular — RelativeLayout para evitar que el FrameLayout cubra los botones.
     Botones anclados al fondo; anillo rellenando la sección superior. -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/sw_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Sección inferior: etiqueta de modo + vueltas + botones (anclados al fondo) -->
    <LinearLayout
        android:id="@+id/sw_bottom_section"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/sw_mode_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="CRONÓMETRO"
            android:textSize="8sp"
            android:textColor="#FF88AAFF"
            android:gravity="center"
            android:letterSpacing="0.15" />

        <TextView
            android:id="@+id/sw_lap_1"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="10sp"
            android:textColor="#FFCCCCCC"
            android:gravity="center"
            android:singleLine="true"
            android:visibility="gone" />

        <TextView
            android:id="@+id/sw_lap_2"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="10sp"
            android:textColor="#FFCCCCCC"
            android:gravity="center"
            android:singleLine="true"
            android:visibility="gone" />

        <TextView
            android:id="@+id/sw_lap_3"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="10sp"
            android:textColor="#FFCCCCCC"
            android:gravity="center"
            android:singleLine="true"
            android:visibility="gone" />

        <!-- Botones normales: ocultos cuando la alarma está sonando -->
        <LinearLayout
            android:id="@+id/sw_btn_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="4dp">

            <ImageButton
                android:id="@+id/btn_reset"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/ic_sw_reset"
                android:background="@android:color/transparent"
                android:scaleType="fitCenter"
                android:contentDescription="Reset" />

            <ImageButton
                android:id="@+id/btn_start_pause"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:src="@drawable/ic_sw_play"
                android:background="@android:color/transparent"
                android:scaleType="fitCenter"
                android:contentDescription="Play/Pause" />

            <ImageButton
                android:id="@+id/btn_toggle_mode"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/ic_sw_swap"
                android:background="@android:color/transparent"
                android:scaleType="fitCenter"
                android:contentDescription="Cambiar modo" />

        </LinearLayout>

        <!-- Botón apagar alarma: solo visible cuando el temporizador llega a cero -->
        <ImageButton
            android:id="@+id/btn_stop_alarm"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginTop="4dp"
            android:src="@drawable/ic_sw_alarm_off"
            android:background="@android:color/transparent"
            android:scaleType="fitCenter"
            android:visibility="gone"
            android:contentDescription="Apagar alarma" />
    </LinearLayout>

    <!-- Anillo + tiempo: ocupa todo el espacio encima de la sección inferior.
         layout_above garantiza que NO se solapa con los botones. -->
    <FrameLayout
        android:id="@+id/sw_ring_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_above="@id/sw_bottom_section">

        <ImageView
            android:id="@+id/sw_ring"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitCenter"
            android:contentDescription="Progreso" />

        <TextView
            android:id="@+id/sw_time"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="00:00"
            android:textSize="18sp"
            android:textColor="#FFFFFFFF"
            android:textStyle="bold"
            android:singleLine="true" />

    </FrameLayout>

</RelativeLayout>
